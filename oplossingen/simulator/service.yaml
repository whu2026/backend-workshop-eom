apiVersion: v1
kind: Service
metadata:
  name: sensor-simulator-service-voorbeeld
  namespace: backend-workshop
spec:
  selector:
    app: sensor-simulator-voorbeeld
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP
